
{{outlet}}

<ol id="todo-list" dir="rtl">
  {{#each model as |todo index|}}

        {{todo-item task=todo index=index class=(concat "todo-list " (if todo.isDone 'task-done' )) editDescription=false  tasks=model}}

  {{/each}}
</ol>
{{!-- {{/if}} --}}

